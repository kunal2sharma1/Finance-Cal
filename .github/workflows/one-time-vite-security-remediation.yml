name: Deprecated security remediation

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deprecated. Release branch watchdog owns dependency remediation."
